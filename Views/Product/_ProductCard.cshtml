@model Product

<div>
    <h2>@Model.ProductName</h2>
    <p>@Model.Price.ToString("c")</p>
    <a class="btn btn-sm btn-info" asp-action="Get" asp-route-id="@Model.ProductId">Get Details</a>
    <hr>
</div>